public static class polyMath {
    class term {
        char variable;
        int coefficient;
        int exponent;

        public term(char v, int c, int e) {
            this.variable = v;
            this.coefficient = c;
            this.exponent = e;
        }
    }
}